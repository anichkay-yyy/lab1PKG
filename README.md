# Конвертер цветов RGB ↔ LAB ↔ CMYK (ColorSearcher)

## Описание
Веб-приложение позволяет пользователю задавать цвет в трех системах:
- **RGB** (красный, зеленый, синий)
- **LAB** (светлота, зелено-красный, сине-желтый)
- **CMYK** (циан, маджента, желтый, ключ/черный)

Файл **color.js** содержит полный функционал приложения преобразования цвета между моделями **RGB**, **LAB**, **CMYK** с живой синхронизацией всех значений и предпросмотром.

Структура файла разделена на логические модули:

---

## ====================

## 🎨 GAMMA

## ====================

Функции:

### `gammaInverse(x)`

* Input: число `x` ∈ [0..1]
* Output: линейное значение RGB после обратной гаммы.
* Описание: Преобразует sRGB компоненту из гамма-кодированного пространства в линейное. Используется перед расчётом XYZ.

### `gammaForward(x)`

* Input: линейное значение RGB ∈ [0..1]
* Output: гамма-кодированная компонентa RGB
* Описание: Прямое обратное преобразование. Применяется после получения линейного RGB перед выводом на экран.

**Назначение блока:** обеспечить корректный перевод между реальным sRGB и линейной моделью при вычислении XYZ и обратных конверсиях.

---

## ====================

## 🟦 LAB base functions

## ====================

### `f(t)`

* Input: нормализованная координата XYZ
* Output: функция преобразования для LAB
* Использует разную формулу в зависимости от порогового значения 0.008856

### `fInv(t)`

* Input: компонент функции f
* Output: восстановленная координата XYZ

**Назначение:** математическая основа LAB модели — нелинейная функция CIE.

---

## ====================

## ⚪ White point D65

## ====================

Константа:

```js
const whitePoint = { X:0.95047, Y:1.0000, Z:1.08883 }
```

* Используется в XYZ↔LAB преобразованиях
* Представляет нормированный белый D65 для sRGB, официальное значение.

---

## ====================

## 🔄 RGB → XYZ

## ====================

### `rgbToXyz(r, g, b)`

* Input: r,g,b ∈ 0..255
* Output: объект `{X, Y, Z}`
* Алгоритм:

  1. нормализация
  2. гамма-инверс
  3. умножение на матрицу линейного преобразования sRGB → XYZ

**Назначение:** переход из RGB в абсолютное цветовое пространство.

---

## ====================

## 🔄 XYZ → RGB

## ====================

### `xyzToRgb(X, Y, Z)`

* Input: координаты XYZ
* Output: объект `{r,g,b}` округлённые 0..255
* Включает:

  * линейное умножение XYZ→RGB
  * гамма-коррекцию
  * ограничение диапазона 0..255
  * вызов `updateCircleColor`

**Особенность:** цвет-предпросмотр обновляется сразу после пересчёта.

---

## ====================

## 🔄 XYZ → LAB

## ====================

### `xyzToLab(X, Y, Z)`

* Input: координаты XYZ
* Output: `{L, a, b}`
* Применяет нормализацию по белой точке и нелинейную функцию `f()`.

---

## ====================

## 🔄 LAB → XYZ

## ====================

### `labToXyz(L, a, b)`

* Input: LAB значения
* Output: XYZ
* Восстанавливает XYZ через `fInv()` и масштабирование белой точкой.

---

## ====================

## 🔄 RGB → LAB

## ====================

### `rgbToLab(r,g,b,draw)`

* Комбинация rgbToXyz → xyzToLab
* Если `draw === true`, значения LAB обновляются в интерфейсе
* Предпросмотр обновляется RGB-параметрами

---

## ====================

## 🔄 LAB → RGB

## ====================

### `labToRgb(L,a,b,draw)`

— Комбинация labToXyz → xyzToRgb
— При draw обновляет RGB поля формы

---

## ====================

## 🔄 RGB → CMYK

## ====================

### `rgbToCmyk(r,g,b,draw)`

* Input: RGB
* Output: массив `[C,M,Y,K]`
* Если цвет абсолютно чёрный (`k=1`), принудительно устанавливает остальное в 0
* Все каналы округлены до четырёх знаков

---

## ====================

## 🔄 CMYK → RGB

## ====================

### `cmykToRgb(c,m,y,k,draw)`

* Output: объект с r,g,b
* Стандартная формула (без gamma, CMYK)
* Обновляет RGB инпуты при необходимости

---

## ====================

## 🔄 CMYK ↔ LAB

## ====================

### `labToCmyk(L,a,b,draw)`

— LAB → RGB → CMYK

### `cmykToLab(c,m,y,k,draw)`

— CMYK → RGB → LAB

**Назначение:** LAB и CMYK не связаны напрямую, всегда используются промежуточные RGB.

---

## ====================

## ✅ VALIDATION

## ====================

### `rgbValidate(evt)`

Проверяет число 0–255.

### `cmykValidate(evt)`

Проверяет число 0–1.

### `labValidate(evt, isL)`

* L: 0–100
* a,b: –128…+127

Если ввод некорректный — очищает поле.

**Назначение:** предотвращает некорректные вычисления и NaN.

---

## ====================

## 🔁 UPDATE CIRCLE COLOR

## ====================

### `updateCircleColor(r,g,b)`

Меняет цвет предпросмотра `.circle`.
Используется *во всех* основополагающих функций.

---

## ====================

## 🔁 UPDATE SYSTEM

## ====================

### `updateColors(evt, system)`

Главный роутер синхронизации моделей.

* Определяет, какая система была изменена (`rgb`, `lab`, `cmyk`)
* Для неё запускает пересчёт оставшихся двух
* Использует таблицу функций:

```js
{
  rgb: [rgbToLab, rgbToCmyk],
  lab: [labToRgb, labToCmyk],
  cmyk: [cmykToLab, cmykToRgb]
}
```

---

## ====================

## 🎧 EVENTS

## ====================

Привязка слушателей ко всем input-полям.

При каждом изменении:

1. Валидируется ввод
2. Пересчитываются остальные модели
3. Обновляется предпросмотр

---

# 🎨 color.html — основные элементы

* Блоки ввода:

  * `<div id="rgbInput">`
  * `<div id="labInput">`
  * `<div id="cmykInput">`
* Элемент предпросмотра:

  * `<div class="circle">`
* Секция документации
* Ссылки «назад» на индекс

---

# 🎛 color.css — основные структуры

* адаптивное позиционирование
* flex-сетка
* стилизация инпутов
* оформления предпросмотра
* темный фон для контраста

---
